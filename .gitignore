# Local configuration — contains machine-specific paths, never commit.
# (config/env.sh.example IS tracked; only the generated config/env.sh is ignored.)
config/env.sh

# Pre-built vthread-trace JDK downloaded + extracted by setup.sh
/.vthread-jdk/

# Java compilation output
java/out/
target/
*.class

# bpftrace temp files
/tmp/correlate.*.bt

*.so

.claude
.vscode

# External helper clones
tools/
# result/